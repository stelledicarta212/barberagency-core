# Tareas del dia 18-05-26

## Objetivo principal
- Cerrar el pendiente heredado del 16-05-26:
  - homologar `v4`, `v6` y `v7` con la misma logica de produccion ya validada en `v2`, `v3` y `v5`;
  - comprobar que las seis plantillas publicas consumen la fuente de verdad de forma consistente.

## Punto de partida heredado del 16-05-26
- Ya quedaron validadas para produccion:
  - `v2`
  - `v3`
  - `v5`
- Ya quedo validado el router publico:
  - `BarberAgency Public Router v5`
  - version tecnica `0.5.0`
- Ya quedo definido que la fuente publica canonica debe gobernar:
  - perfil publico;
  - branding;
  - plantilla;
  - servicios;
  - barberos;
  - horarios;
  - URL publica;
  - QR.
- Ya se comprobo que:
  - el editor no debe reconstruir URLs por su cuenta;
  - el boton `Ver landing publicada` debe usar solo la URL devuelta por la RPC actual;
  - las bajas y altas de barberos/servicios deben reconciliarse contra la fuente de verdad.

## Pendiente real para hoy
- Aplicar la misma logica ya validada en `v2`, `v3` y `v5` a:
  - `v4`
  - `v6`
  - `v7`
- Verificar que ninguna de esas tres plantillas:
  - dependa de seeds viejos;
  - sobrescriba datos con endpoints legacy;
  - deje QR o URL en estado `Cargando`;
  - muestre nombre tecnico cuando deba mostrar `nombre_publico`.

## Forma de trabajo acordada
1. Revisar primero cada plantilla en codigo.
2. Corregir una plantilla por vez.
3. Probar primero en Postman.
4. Luego probar en WordPress/navegador.
5. No cerrar una plantilla sin evidencia.
6. Mantener siempre la BD y la RPC publica como fuente de verdad.

## Bloques propuestos para hoy

### Bloque 1 - Revision comparativa
- Comparar `v4`, `v6` y `v7` contra el comportamiento ya correcto de:
  - `v2`
  - `v3`
  - `v5`
- Identificar por plantilla:
  - hidratacion publica;
  - lectura de `BA_LANDING_ROUTE_CONTEXT`;
  - lectura del slug;
  - uso de endpoints legacy;
  - render de QR/URL;
  - nombre visible;
  - branding.

### Bloque 2 - Homologacion tecnica
- Aplicar en cada plantilla pendiente:
  - fuente publica como unica verdad;
  - reintento controlado de hidratacion si aplica;
  - eliminacion de fallbacks inseguros;
  - nombre publico consistente;
  - QR y URL derivados del contexto canonico.

### Bloque 3 - Pruebas E2E
- Probar primero en Postman:
  - publicacion;
  - lectura publica;
  - resolucion QR.
- Luego probar en WordPress:
  - landing final;
  - cambio de plantilla;
  - altas y bajas de barberos;
  - altas y bajas de servicios;
  - QR;
  - URL publica;
  - nombre visible.

## Criterio de cierre de hoy
- `v4`, `v6` y `v7` deben quedar con la misma logica de produccion que `v2`, `v3` y `v5`.
- Solo despues de pruebas completas en Postman y WordPress se podra marcar:
  - `v2`
  - `v3`
  - `v4`
  - `v5`
  - `v6`
  - `v7`
  como listas para produccion.

## Avance del dia

### `v4` - revision y prueba
- Revision de codigo:
  - `v4` ya traia la misma base tecnica validada en `v5`:
    - lectura de `BA_LANDING_ROUTE_CONTEXT`;
    - slug desde `/b/{slug}`;
    - bloqueo de seeds viejos en ruta publica;
    - QR y URL construidos desde contexto canonico;
    - `nombre_publico` como valor preferente.
- Prueba funcional:
  - se probo `v4` en flujo real;
  - confirmacion del usuario:
    - hidrata correctamente los datos de la fuente de verdad.
- Resultado:
  - `v4` queda validada para produccion sin requerir cambios adicionales de codigo.

### `v6` - revision y prueba
- Revision de codigo:
  - se confirmo que `v6` ya habia recibido previamente la misma base tecnica de hidratacion publica usada por `v5`;
  - conserva:
    - lectura de `BA_LANDING_ROUTE_CONTEXT`;
    - slug desde `/b/{slug}`;
    - bloqueo de seeds viejos en ruta publica;
    - QR y URL desde el contexto canonico;
    - `nombre_publico` como valor preferente.
- Prueba funcional:
  - se probo `v6` en flujo real;
  - confirmacion del usuario:
    - hidrata correctamente los datos de la fuente de verdad.
- Resultado:
  - `v6` queda validada para produccion.

### `v7` - revision y prueba
- Revision de codigo:
  - `v7` ya traia la logica publica canonica necesaria:
    - lectura de `BA_LANDING_ROUTE_CONTEXT`;
    - slug desde `/b/{slug}`;
    - bloqueo de seeds viejos en ruta publica;
    - QR y URL desde contexto canonico;
    - lectura de `nombre_publico`.
  - mantiene una capa propia de branding con `applyLandingBranding(...)`,
    pero sin romper la fuente de verdad.
- Prueba funcional:
  - se probo `v7` en flujo real;
  - se creo un barbero nuevo;
  - confirmacion del usuario:
    - la hidratacion fue correcta y tomo el nuevo barbero desde la fuente de verdad.
- Resultado:
  - `v7` queda validada para produccion.

## Cierre del pendiente heredado del 16-05-26
- Plantillas ya validadas para produccion:
  - `v2`
  - `v3`
  - `v4`
  - `v5`
  - `v6`
  - `v7`
- Conclusion:
  - las seis plantillas publicas ya consumen correctamente la fuente de verdad;
  - todas quedaron homologadas en comportamiento publico;
  - el siguiente frente ya no es la hidratacion de plantillas, sino mejoras de calidad de datos,
    empezando por direccion y ubicacion de Google Maps en el registro de barberia.

## Nuevo frente - direccion + Google Maps para produccion

### Hallazgo
- El sistema ya manejaba correctamente `direccion` como dato publico.
- El editor y las plantillas ya sabian consumir:
  - `maps_url`
  - `map_url`
  - `location_map_url`
  - `location_address`
- Sin embargo, el registro inicial de barberia solo capturaba `direccion`.
- Consecuencia:
  - el editor podia construir un mapa a partir del texto de la direccion;
  - pero no existia un campo oficial de fuente de verdad para conservar una URL precisa de Google Maps cuando el usuario la tuviera.

### Decision de produccion
- Mantener dos datos separados:
  - `direccion`: texto visible y obligatorio;
  - `maps_url`: URL opcional de Google Maps para mejorar precision.
- Ambas formas deben funcionar:
  - si el usuario solo escribe direccion, el sistema sigue funcionando;
  - si tambien pega URL de Google Maps, esa URL debe preferirse y conservarse.

### Cambios locales aplicados
- `registrobarberia.html`
  - agrega captura opcional para `barberia.maps_url`;
  - deja direccion y URL de Google Maps en el mismo paso visual;
  - muestra ayuda indicando que la URL mejora precision;
  - agrega CTA `Haz clic aqui para abrir Google Maps`;
  - valida URLs compatibles de Google Maps;
  - conserva `maps_url` en seed, review y payload de onboarding.
- `registro-barberia.md`
  - agrega estilo para que el CTA de Google Maps se vea dorado por defecto;
  - cambia a azul al pasar el mouse o recibir foco.
- `landing_editor_v2_unico_vscode.html`
  - conserva la URL de Maps heredada desde registro/seed;
  - evita reemplazarla siempre por una URL generada desde texto cuando ya existe una URL real.

### Cambios ya hechos en BD / fuente de verdad
- `barberia_public_profiles`
  - se agrego la columna `maps_url text`.
- `v_barberia_public_landing`
  - se actualizo para exponer `maps_url` como nueva ultima columna.
- `ba_publicar_landing_completa(jsonb)`
  - ahora recibe y persiste `maps_url` desde:
    - `profile.maps_url`
    - `profile.map_url`
    - `profile.location_map_url`
    - `barberia.maps_url`
    - `barberia.map_url`
    - `barberia.location_map_url`
    - raiz del payload.
- `ba_get_landing_publica(text)`
  - ahora devuelve `perfil_publico.maps_url`.

### Evidencia ya validada
- La columna `maps_url` quedo creada en `barberia_public_profiles`.
- La vista publica expone `maps_url` como columna 21.
- La funcion de publicacion ya contiene:
  - variable `v_maps_url`;
  - persistencia `maps_url = EXCLUDED.maps_url`.
- `ba_get_landing_publica('barberia-146')`
  - responde `200 OK`;
  - ya devuelve `perfil_publico.maps_url`;
  - en barberia existente aun aparece `null` porque falta republicarla con URL real de Google Maps.

### Pendiente antes de cerrar este frente
- Probar con una URL real de Google Maps en el flujo completo:
  1. registro / captura;
  2. publicacion;
  3. persistencia en `barberia_public_profiles.maps_url`;
  4. lectura por `ba_get_landing_publica`;
  5. render correcto en editor y landing publica.
- Solo despues de esa prueba:
  - copiar definitivamente los cambios a WordPress;
  - confirmar que ambas opciones funcionan:
    - solo direccion;
    - direccion + URL de Google Maps.
