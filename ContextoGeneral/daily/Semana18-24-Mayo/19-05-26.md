# Tareas del dia 19-05-26

## Objetivo principal
- Cerrar el frente de direccion + Google Maps iniciado el 18-05-26.
- Validar que `maps_url` ya funciona de punta a punta desde registro hasta landing publica.

## Punto de partida
- Ayer quedo implementado y documentado:
  - columna `barberia_public_profiles.maps_url`;
  - vista `v_barberia_public_landing` exponiendo `maps_url`;
  - RPC `ba_publicar_landing_completa(jsonb)` persistiendo `maps_url`;
  - RPC `ba_get_landing_publica(text)` devolviendo `perfil_publico.maps_url`;
  - registro con direccion obligatoria y URL de Google Maps opcional en el mismo paso;
  - CTA `Haz clic aqui para abrir Google Maps`;
  - editor conservando URL real de Maps si viene desde registro/seed.

## Pendiente real para hoy
- Probar con una URL real de Google Maps:
  1. registrar/publicar una barberia con direccion + `maps_url`;
  2. verificar en Postman que `ba_publicar_landing_completa` persiste `maps_url`;
  3. verificar en BD `barberia_public_profiles.maps_url`;
  4. verificar en Postman `ba_get_landing_publica`;
  5. verificar en WordPress/editor;
  6. verificar landing publica.

## Criterio de cierre
- Deben funcionar dos escenarios:
  - solo direccion;
  - direccion + URL de Google Maps.
- La URL de Google Maps debe venir de la fuente de verdad, no de seed local.
- La landing publica debe renderizar mapa/ubicacion sin romper QR, URL publica, servicios, barberos ni horarios.

## Orden de trabajo
1. Prueba en Postman.
2. Verificacion en pgAdmin si hace falta.
3. Prueba en WordPress.
4. Documentar evidencia final.

## Avance: simplificacion para usuario
- Decision de UX: mantener un solo campo obligatorio para el usuario: `direccion`.
- `maps_url` queda como mejora opcional para mayor precision.
- Comportamiento esperado:
  - si solo hay direccion, la landing/editor deben construir ubicacion con direccion;
  - si hay direccion + URL de Google Maps, se usa `maps_url` como fuente mas precisa;
  - si no hay `maps_url`, no debe romper mapa, QR ni landing publica.
- No se cambia la fuente de verdad:
  - `barberia.direccion` sigue siendo el dato base;
  - `barberia.maps_url` sigue viajando al payload y se persiste en `barberia_public_profiles.maps_url`.
- Ajuste realizado en registro:
  - texto del paso 3 mas simple;
  - se retiro la casilla visible de URL de Google Maps para simplificar el flujo;
  - `maps_url` queda interno/opcional para compatibilidad con la fuente de verdad;
  - ayuda adicional: si no ve el punto correcto, el usuario puede abrir Google Maps, buscar el local y pegar la ubicacion exacta;
  - CTA dorado se mantiene para abrir Google Maps.

## Avance: confirmacion visual de ubicacion
- Se agrego vista previa de Google Maps en el paso de direccion del registro.
- Flujo esperado:
  1. el usuario escribe direccion;
  2. puede tocar `Ver mapa`;
  3. revisa la vista previa;
  4. si esta correcta, toca `Confirmar ubicacion`;
  5. si no esta correcta, corrige direccion o pega URL de Google Maps.
- La logica de datos no cambia:
  - `direccion` sigue obligatoria;
  - `maps_url` sigue opcional;
  - el payload conserva `barberia.direccion` y `barberia.maps_url`.
- Ajuste visual movil: los botones `Ver mapa` y `Confirmar ubicacion` quedan en grid con `gap: 20px`.
- Correccion: el CSS de separacion tambien se agrego directamente en `project/templates/plantillas/registrobarberia.html`, porque ese es el HTML que se esta pegando en WordPress.
- Mantenimiento: `registrobarberia.html` quedo dividido por secciones comentadas para facilitar lectura y edicion futura.

## Evidencia final: CSS real en WordPress
- Se confirmo que el CSS activo de `registrobarberia` no esta dentro del archivo `.md`; esta en el Custom CSS de Elementor.
- El archivo `.md` queda como referencia/documentacion, pero el codigo operativo en WordPress es:
  - HTML pegado desde `project/templates/plantillas/registrobarberia.html`;
  - CSS pegado en Custom CSS de Elementor.
- Hallazgo:
  - el espacio entre `Ver mapa` y `Confirmar ubicacion` no se veia en WP porque faltaba aplicar el ajuste en el CSS real de Elementor.
- Solucion aplicada:
  - se agrego bloque CSS final para `.ba-location-preview`, `.ba-location-link` y `.ba-actions-row`;
  - `.ba-actions-row` usa `gap: 20px !important`;
  - en movil `.ba-actions-row` pasa a grid con `gap: 20px !important`;
  - el enlace de Google Maps queda dorado fijo y en hover cambia a azul tipo enlace.
- Resultado verificado por usuario:
  - el mapa se ve por defecto;
  - el flujo de ubicacion queda claro;
  - los botones ya tienen separacion correcta en movil;
  - la UI queda lista para seguir con pruebas de Google Maps sin afectar la fuente de verdad.

## Estado de cierre del dia
- Registro barberia queda con UX simplificada:
  - un solo campo principal de direccion;
  - mapa visible por defecto;
  - CTA para abrir Google Maps;
  - confirmacion visual antes de continuar.
- Pendiente para el siguiente bloque:
  - pruebas completas de Google Maps en Postman/BD/WP;
  - validar los dos escenarios: solo direccion y direccion con URL precisa de Google Maps.

## Nuevo frente revisado: imagenes para servicios y barberos
- Se reviso como carga actualmente la imagen del logo en el editor.
- Hallazgo tecnico:
  - el logo no se guarda como archivo local ni base64;
  - el editor usa upload por n8n hacia storage;
  - endpoint principal:
    - `https://barberagency-n8n.gymh5g.easypanel.host/webhook/barberagency/landing/upload-live`;
  - endpoints fallback:
    - `https://barberagency-n8n.gymh5g.easypanel.host/webhook/barberagency/landing/upload-v2`;
    - `https://barberagency-n8n.gymh5g.easypanel.host/webhook/barberagency/landing/upload`;
  - el frontend envia `FormData` con:
    - `file`;
    - `biz_slug`;
  - n8n devuelve una URL publica;
  - el editor guarda esa URL en `logo_url` o `cover_url`;
  - al publicar, esa URL se persiste en BD y luego la landing publica la lee desde fuente de verdad.

## Decision de arquitectura para imagenes
- Toda imagen nueva debe seguir la misma logica del logo:
  - subir archivo a n8n/storage;
  - recibir URL publica;
  - guardar solo la URL publica en BD;
  - leer desde fuente de verdad en landing publica/editor.
- No se debe usar:
  - base64 en BD;
  - blob local como valor final;
  - imagenes que vivan solo en `sessionStorage` o seed.

## Estado actual de BD para este frente
- Segun schema local revisado:
  - `barberos` tiene: `id`, `barberia_id`, `nombre`, `activo`;
  - `servicios` tiene: `id`, `barberia_id`, `nombre`, `duracion_min`, `precio`.
- Falta persistencia de imagenes en fuente de verdad:
  - falta `barberos.foto_url`;
  - falta `servicios.imagen_url`.

## Solucion propuesta para produccion
1. Agregar columnas en BD:
   - `barberos.foto_url text`;
   - `servicios.imagen_url text`.
2. Actualizar RPC de sincronizacion de colecciones:
   - recibir `foto_url` en barberos;
   - recibir `imagen_url` en servicios;
   - hacer upsert/desactivacion sin perder esas URLs.
3. Actualizar `registrobarberia.html`:
   - en servicios: agregar opcion opcional `Subir imagen del servicio`;
   - en barberos: mantener avatares y agregar opcion `Subir foto`;
   - si barbero sube foto, gana la foto subida;
   - si no sube foto, queda el avatar seleccionado;
   - usar el mismo flujo `FormData(file, biz_slug)` hacia n8n/storage.
4. Actualizar payload:
   - servicios deben viajar con `imagen_url`;
   - barberos deben viajar con `foto_url` o `foto` normalizada a `foto_url`.
5. Probar primero en Postman:
   - publicar barberia con servicio con imagen;
   - publicar barberia con barbero con foto subida;
   - publicar barbero solo con avatar;
   - verificar BD;
   - verificar `ba_get_landing_publica`.
6. Solo al final pegar cambios en WordPress.

## Regla de produccion acordada
- Cualquier imagen que el usuario suba debe ir a storage con la misma logica del logo.
- La fuente de verdad final debe ser BD, no el editor ni el navegador.
